# MPC Wallet Secrets — TEMPLATE ONLY
# In production, use sealed-secrets or external secrets operator.
# DO NOT commit real secrets to version control.
apiVersion: v1
kind: Secret
metadata:
  name: mpc-secrets
  namespace: mpc-wallet
type: Opaque
stringData:
  jwt_secret: "REPLACE_WITH_PRODUCTION_SECRET"
  api_keys: "REPLACE_WITH_PRODUCTION_API_KEYS"
  encryption_password: "REPLACE_WITH_KEY_STORE_PASSWORD"
